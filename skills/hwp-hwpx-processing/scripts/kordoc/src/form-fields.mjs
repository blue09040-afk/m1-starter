import { extractClickHereFields, extractFormSchema, parse } from "kordoc"
function toArrayBuffer(bytes){return bytes.buffer.slice(bytes.byteOffset,bytes.byteOffset+bytes.byteLength)}
export async function analyzeFormFields(bytes){const arrayBuffer=toArrayBuffer(bytes);const [fields,parsed]=await Promise.all([extractClickHereFields(arrayBuffer),parse(arrayBuffer)]);const counts=new Map();for(const item of fields){const field=String(item.name||"").trim();if(!field)continue;counts.set(field,(counts.get(field)??0)+1)}const formFields=[...counts.keys()].sort();let labelFormFieldCount=0;let emptyLabelFormFieldCount=0;let labelFormConfidence=0;if(parsed.success&&parsed.fileType==="hwpx"){const schema=extractFormSchema(parsed.blocks);labelFormConfidence=schema.confidence;labelFormFieldCount=schema.fields.length;emptyLabelFormFieldCount=schema.fields.filter((field)=>field.empty).length}return{formFields,formFieldOccurrences:Object.fromEntries(formFields.map((field)=>[field,counts.get(field)])),duplicateFormFields:formFields.filter((field)=>counts.get(field)>1),labelFormFieldCount,emptyLabelFormFieldCount,labelFormConfidence}}
