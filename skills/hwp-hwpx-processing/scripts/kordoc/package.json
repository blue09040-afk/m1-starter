{
  "name": "hwpx-writing-starter",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Lightweight Kordoc wrapper for HWPX writing, templating, patching, filling, collecting, and validation.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "check": "node --check src/core.mjs && node --check src/cli.mjs && node --check src/hwpx-package.mjs && node --check src/form-fields.mjs && node --check test/smoke.mjs && node --check test/cli-smoke.mjs && node --check test/form-fields.mjs && node --check test/package-quality.mjs",
    "test": "node --test test/*.mjs"
  },
  "dependencies": {
    "@xmldom/xmldom": "0.9.12",
    "jszip": "3.10.1",
    "kordoc": "4.12.0"
  },
  "overrides": {
    "adm-zip": "^0.6.0",
    "sharp": "^0.35.0"
  }
}
